Create a `wire_assign.cpp` file that implements a direct wire assignment kernel.

Objective:

- assign the output directly from the input
- no extra logic

Function prototype:

```cpp
void wire_assign(input_t in, output_t* out);
```

Behavior:

- the implementation should perform `out = in`
- the file should include the matching header for the type definitions
