Create a and_gate.cpp file with the following specifications:

Objective: 
Implement a High-Level Synthesis (HLS) function that performs a bitwise AND operation, where the output is the logical AND of two inputs (out = a & b).

Function Prototype:
```cpp
void and_gate(input_t a, input_t b, output_t* out);
```

Requirements:
Include Necessary Headers:
    Include the header file that provides the definitions of input_t and output_t. These data types represent single-bit signals and are defined elsewhere (e.g., in and_gate.h).

Function Implementation:
    Implement the function and_gate such that it assigns the logical AND of inputs a and b to the output pointer out.
    The function should directly perform the bitwise AND operation without any additional logic or processing.

Coding Standards:
    Ensure that the code is clear, concise, and adheres to HLS coding conventions.
    Include any necessary comments to explain the code for readability.

Task:
Provide the complete content of the and_gate.cpp file based on the above specifications.