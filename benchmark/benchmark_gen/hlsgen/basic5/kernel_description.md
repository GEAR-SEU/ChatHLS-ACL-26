Create a zerolow.cpp file with the following specifications:

Objective:

Implement a High-Level Synthesis (HLS) function that always outputs a LOW signal (zero), meaning the output is constantly set to logic LOW.

Function Prototype:

<CPP>
void zerolow(bool* zero);
Requirements:

1. Include Necessary Headers:

Include the header file that provides the definition of output_t. This data type represents a single-bit signal and is defined elsewhere (e.g., in zero.h).
Include any other standard headers required for HLS projects.
2. Function Implementation:

Implement the function zero such that it assigns a logic LOW (zero) to the output pointer zero.
The function should directly perform the assignment without any additional logic or processing.
3. Coding Standards:

Ensure that the code is clear, concise, and adheres to HLS coding conventions.
Use appropriate data types for single-bit signals (e.g., bool or ap_uint<1>).
Include necessary comments to explain the code for readability.
Task:

Provide the complete content of the zerolow.cpp file based on the above specifications.****